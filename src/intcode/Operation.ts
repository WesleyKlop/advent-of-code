// Operation types
export enum Operation {
    ADD = 1,
    TIMES = 2,
    INPUT = 3,
    OUTPUT = 4,
    JUMP_IF_TRUE = 5,
    JUMP_IF_FALSE = 6,
    LOWER_THAN = 7,
    EQUALS = 8,
    SET_BASE = 9,
    HALT = 99,
}
